# For some reason more traditional config with application name in bootstrap keeps eureka registered itself as a client
spring:
  cloud:
    config:
      name: discovery-service
      uri: http://localhost:8888